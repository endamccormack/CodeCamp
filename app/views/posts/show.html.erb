<p id="notice"><%= notice %></p>
<span class="controls">
<% if admin_signed_in? %>
<%= link_to image_tag("edit.png", :border => 0), edit_post_path(@post)  %> 
<% end %>
<%= link_to '←', posts_path %>
</span>
<h1><%= @post.title %></h1>

<span><%= @post.created_at.utc.strftime("%a, %b %e, %Y")  %></span>


<p>
  <%= simple_format(@post.content) %>
</p>

